// import { Request, Response } from 'express';
// import loginService from '../services/loginService'

// export default class insertLoginController {
//     static async pagarPix(req: Request, res: Response) {
//       const { idConta, valor, chavePix, descricao } = req.body;
//       await PagamentoService.pagarPix(
//         idConta,
//         valor,
//         chavePix,
//         descricao,
//       );
//       res.status(200).json({ mensagem: 'Pagamento efetuado com sucesso' });
//     }
//   }
